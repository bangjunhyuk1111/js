<!-- 모든상품 보기 -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Products</title>
</head>
<body>
    <h1><%= category %> Products!!!</h1>
    <ul>
        <% for(let product of products) { %> 
            <!-- <li><%= product.name %></li> -->
            <li><a href="/products/<%=product._id%>"><%= product.name %></a></li>          
            <% }%>
    </ul>
    <a href="/products/new">New Product(새로운 상품 만들기)</a>
    <%if(category !== 'All') { %>
    <a href="/products">All products(모든상품보기)</a>
    <% } %>
</body>
</html>